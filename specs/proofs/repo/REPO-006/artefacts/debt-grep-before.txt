origin/main:AGENTS.md:37:3. **Execute guarded work** — Apply the control matrix below while delivering slice scope. Bring up the docker parity stack via `node tools/dev/parity-stack.mjs ensure` (or the `make docker-dev-detached` wrapper) before running tests or validations so every command executes inside the compose environment. Keep the session’s state recorded in `specs/working-memory/parity-state.json` and refresh the stack only when the parity tool requests it. Long-running dev servers (Next.js, Go API, Storybook, etc.) must launch through `node tools/dev/run-managed.mjs`/Make targets so they never block the console and their logs live under `tmp/process-logs/`. Respect guard enforcement (write permissions, hooks) and keep all edits within the active slice allow list.
origin/main:AGENTS.md:38:4. **Capture proof** — Assemble logs, artefacts, and RESULT markers under `specs/proofs/<domain>/<SLICE-ID>/...`, documenting acceptance review explicitly. Every proof must include parity-stack evidence (docker compose) and production evidence (live environment) for the same scenario, with both contexts referenced in the README.
origin/main:AGENTS.md:54:| Dual-environment enforcement | Run lint/tests/sweeps inside the docker parity stack (`node tools/dev/parity-stack.mjs ensure`, `docker compose ...`) and replicate the validation against production. Log the parity-state snapshot (`specs/working-memory/parity-state.json`) and `docker compose ps` output in your proof bundle. | [Parity stack runbook](specs/runbooks/parity/README.md) |
origin/main:AGENTS.md:55:| Session process control | Every long-running dev command is launched via `node tools/dev/run-managed.mjs` (e.g., `make dev-web`, `make dev-api`) so the console remains free and logs are captured under `tmp/process-logs/` with metadata in `specs/working-memory/dev-processes.json`. | [Automation Matrix](AGENTS/charter/automation-matrix.md#automation-interface); `tools/dev/README.md` |
origin/main:AGENTS.md:56:| Privileged task handoff | Queue every `sudo` command via `node tools/dev/sudo-request.mjs request --command "<cmd>" --reason "<why>"`, deliver the generated script to the steward, and record acknowledgements in `specs/working-memory/sudo-queue.json`. | [Automation Matrix](AGENTS/charter/automation-matrix.md#automation-interface); `tools/dev/README.md` |
origin/main:AGENTS.md:78:1. **Pre-flight governance refresh** — Run `npm run spec:slice-index` and `npm run spec:progress` immediately after completing a deliverable section, restage the regenerated files (`specs/notes/indexes/slice-index.md`, `specs/project-progress/progress-summary.json`), and reference the log under `artefacts/<domain>/<SLICE-ID>/logs/`. *Measurement:* commits never trigger `verify-slice-index` or telemetry drift errors.
origin/main:AGENTS.md:79:2. **Proactive lint/test sweep** — Execute `npm --workspace <app> run test` and `npm run go:test-all` (runs `go test ./...` inside every Go module listed in `go.work`) locally before staging. Capture the outputs inside the artefact tree and cite them in the proof README. *Measurement:* pre-commit hook completes without discovering new lint/test failures.
origin/main:AGENTS.md:83:6. **Session runtime ledger** — Start exactly one docker parity stack per working session via `node tools/dev/parity-stack.mjs ensure` and track every long-lived dev server with `node tools/dev/run-managed.mjs`. Archive the parity status JSON plus the managed-process log you used in the proof artefacts. *Measurement:* `specs/working-memory/parity-state.json` always reflects the running stack, and `specs/working-memory/dev-processes.json` is empty when the session ends.
origin/main:AGENTS.md:84:7. **Privileged command queueing** — Before any `sudo` action, run `node tools/dev/sudo-request.mjs request --command "<cmd>" --reason "<context>"`, hand the generated script to the steward, then confirm with `node tools/dev/sudo-request.mjs ack --id <ID> --notes "<result>"`. Store the console output plus the updated `specs/working-memory/sudo-queue.json` snapshot inside your artefacts. *Measurement:* the queue documents every privileged command and proofs cite the matching entry.
origin/main:AGENTS/charter/automation-matrix.md:20:- `tools/dev/sudo-request.mjs` — queue privileged commands for steward.
origin/main:AGENTS/slices/WEB-024.md:7:- Replace the separated homepage hero background and bicycle layers with one precomposed campaign image.
origin/main:Makefile:1:.PHONY: docker-dev-detached dev-web dev-api
origin/main:Makefile:3:docker-dev-detached:
origin/main:Makefile:9:dev-api:
origin/main:tools/dev/README.md:5:- Sudo queue: `node tools/dev/sudo-request.mjs <request|ack> ...`
