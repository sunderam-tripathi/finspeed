# Proof — DOCS-004 (UI/UX spec references)

## Implementation
- Updated AGENTS contract to state all customer-facing slices must use `_shared/assets/` imagery and follow `specs/references/handoff/ui-ux-aesthetics.md`.
- Refreshed `AGENTS/specs/web-handoff-specs.md` checklist and shared steps to cite the new UI spec instead of the retired brand-style guide, and clarify contract terminology.
- Added `ui-ux-aesthetics.md` to the handoff-guide’s `_shared/` explanation plus tweaked contract descriptions.

## Validation
- Docs-only change; captured `git diff --stat` for evidence (`artefacts/diff.txt`).

## Result
- RESULT: PASS (documentation updated; no runtime changes).
