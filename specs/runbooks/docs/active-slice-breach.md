# Active Slice Breach Runbook

If a guard rejects a commit/push due to out-of-scope changes:
- Review `specs/working-memory/active-slice.json` and the slice allow-list.
- Move unrelated edits to a different slice or expand scope formally.
- Regenerate plan if necessary and reattempt.

