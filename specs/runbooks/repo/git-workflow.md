# Guarded Git Workflow

Checklist:
- Ensure active slice set and plan exists.
- Stage only files allowed by the active slice.
- Commit message: include slice ID and concise scope.
- Push after pre-push hook passes.
- Park slice if work is complete.

