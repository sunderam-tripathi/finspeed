# Plan — WEB-002

- Context:
  SCN-001 includes contract plan scenarios ensuring hero copy, language toggle (IC-6), and navigation CTA (IC-8). We need automated tests so regressions are caught locally/CI.
- Goals:
  - Add Playwright test harness within `apps/web` with CI integration.
  - Encode core contract scenarios (hero tagline, bilingual toggle, dealer CTA, support links) from `tests-and-runbook/contract-plan.md`.
  - Capture logs in proof bundle and refresh telemetry.
- Risks:
  - Dev server spin-up for Playwright could slow CI; mitigate via reuseExistingServer and limited browsers.
  - Tests rely on accessible labels; ensure components expose stable text/test IDs.

## Steps
1. Update web spec/ledger and add slice supplement (done).
2. Install `@playwright/test`, add `playwright.config.ts`, add `npm run test` script under `apps/web`.
3. Introduce component tweaks for deterministic selectors (e.g., `aria-labels`, `data-testid`).
4. Write contract tests per runbook (hero, toggles, CTA) and ensure they pass locally.
5. Add CI step (guard workflow) to run `npm run test -w web`, capture logs in proof artefacts, refresh telemetry, park slice.

## Execution Checklist
- [x] Playwright configured.
- [x] Contract tests implemented.
- [x] Proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.

## Artefact References
- Contract plan: `specs/references/handoff/SCN-001-site-structure/tests-and-runbook/contract-plan.md`
- Tests: `apps/web/tests/contract.spec.ts`
- Proof: `specs/proofs/web/WEB-002/README.md`
