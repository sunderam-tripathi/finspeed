# Plan — WEB-016

- Context:
  Domain `finspeed.online` is wired to the Vercel web app but currently fails configuration because Cloudflare DNS does not match the new Vercel records.
- Goals:
  - Document the exact Cloudflare zone settings (records, proxy status, SSL/TLS mode) required so both apex and `www` hosts validate in Vercel.
  - Provide a repeatable verification flow (dns lookups + Vercel refresh) and reference artefacts needed for proof.
- Risks:
  - Cloudflare orange-cloud proxying may break Vercel verification unless we keep records in DNS-only mode.
  - Nameserver changes can take hours to propagate, so comms must highlight timelines and fallback.

## Steps
1. Gather requirements from Vercel (A/CNAME targets, verification guidance) and any repo docs referencing Cloudflare or domain ownership.
2. Produce Cloudflare setup instructions: zone onboarding, record creation, proxy/TLS toggles, and any Zero-Downtime considerations.
3. Outline validation + telemetry actions (dig lookups, Vercel refresh, proof artefacts, guard scripts) and ensure they are logged for the slice.

## Execution Checklist
- [x] Guard alignment confirmed (`node tools/spec/check-active-slice.mjs`).
- [x] Cloudflare DNS + SSL/TLS instructions drafted and stored in repo.
- [x] Verification + telemetry workflow captured (inc. parity stack + production evidence expectations).
- [x] Proof artefact plan (logs, screenshots, RESULT markers) ready before closing slice.

## 2026-07-26 ledger close-out

- Every checklist item was completed when this slice was worked, but the ledger flip
  was never made and the entry sat "active" for months. Marked done retroactively
  during the 2026-07-26 stale-ledger cleanup; no outstanding work was identified.
