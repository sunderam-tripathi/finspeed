# Plan — WEB-005

- Context:
  Dealer locator currently logs analytics to console. SCN-004 contract expects GA4 payloads (site-interaction contract) honoring consent and pushing to dataLayer. We need a dispatcher and tests verifying payload structure.
- Goals:
  - Implement analytics service reading consent flag and pushing events to `window.dataLayer` while preserving console fallback.
  - Provide hook on `/dealers` UI to emit payloads with required fields: event name, postal prefix, radius, channel.
  - Extend Playwright to validate event objects and consent gating.
  - Capture proof artefacts + telemetry.
- Risks:
  - Accessing `window` on SSR: guard in hooks.
  - Playwright event capture must reset between tests to avoid leakage.

## Steps
1. Update spec/ledger and slice supplement (done).
2. Implement analytics dispatcher (with consent stub + dataLayer push) and integrate into `/dealers` page.
3. Expand Playwright suite to inspect `window.dataLayer` entries for search/contact events.
4. Capture logs, refresh telemetry, park slice.

## Execution Checklist
- [ ] Analytics dispatcher implemented.
- [ ] Dealer locator wired to dispatcher.
- [ ] Playwright tests cover payload structure/consent.
- [ ] Proof artefacts captured.
- [ ] Telemetry refreshed.
- [ ] Slice parked.
