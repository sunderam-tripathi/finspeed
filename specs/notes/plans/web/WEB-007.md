# Plan — WEB-007

- Context:
  Dealer locator analytics currently assume consent. SCN-004 requires consent gating per `_shared/contracts/site-interaction-ga4.md`. We need a consent banner + GA4 dispatcher stub.
- Goals:
  - Implement consent state (banner + storage) toggling analytics dispatch.
  - Update analytics service to push to `window.dataLayer` and mock GA endpoint only when consented.
  - Extend Playwright tests to cover consent accept/decline flows.
- Risks:
  - Without a backend, consent data is client-only; document limitation.
  - GA endpoint stub should not slow tests.

## Steps
1. Update spec/ledger and slice supplement (done).
2. Implement consent hook + banner in `apps/web` root (persist choice in localStorage) and wire `/dealers` page to show banner.
3. Enhance analytics service to guard events by consent and log GA payload format.
4. Extend Playwright tests for consent accept/decline scenarios; capture proof artefacts + telemetry; park slice.

## Execution Checklist
- [x] Consent banner + storage implemented.
- [x] Analytics service updated for GA payloads.
- [x] Tests covering consent paths.
- [x] Proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.
