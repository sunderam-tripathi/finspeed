# Plan — WEB-012

- Context:
  Dealer locator currently uses a static SVG map. SCN-004 expects interactive map with live data. This slice integrates geoJSON-based pins and selection.
- Goals:
  - Import geoJSON (sample) and render pins using a lightweight map component.
  - Sync selection with cards, display radius overlays, respect outage state.
  - Capture screenshots/logs + lint/test evidence.
- Risks:
  - Without Leaflet, we’ll emulate map interactions; document limitations.

## Steps
1. Extend data file with geoJSON and create map component enhancements.
2. Update `/dealers` to load data async, animate pins, show radius per selection.
3. Update tests, capture proof, refresh telemetry, park slice.

## Execution Checklist
- [x] GeoJSON data + loader added.
- [x] Map + page updated.
- [x] Tests/lint + proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.
