# Plan — WEB-003

- Context:
  SCN-004 defines the dealer locator experience (IC-12) with postal validation, 20km radius default, filters, outage banner, and support contact. WEB-003 delivers the `/dealers` page shell with data seeded from the handoff pack.
- Goals:
  - Parse a subset of dealer data (static JSON) from `specs/references/handoff/SCN-004-dealer-locator/data/` and expose search/filter helpers.
  - Implement `/dealers` route with postal input, radius display, filter chips (services), result list with CTA buttons, and fallback/outage banners referencing support contact info.
  - Provide analytics stub functions (`logAnalyticsEvent`) for `dealer_search_submitted`, `dealer_directions_click`, `dealer_contact_action`.
  - Document behaviour and capture proof artefacts (screenshots/logs + telemetry).
- Risks:
  - Map integration deferred; ensure UI communicates placeholder state.
  - Without actual backend, search results rely on static dataset; note limitations in proof.

## Steps
1. Activate slice (done), create data + utilities referencing SCN-004 assets.
2. Implement `/dealers/page.tsx` with search form, validation (6-digit postal), 20km default radius, filter chips, results grid.
3. Add outage toggle + fallback messaging referencing support contact; log analytics stubs on interactions.
4. Update nav CTA to route to `/dealers` (already pointing) and ensure tests cover new page (manual for now).
5. Capture artefacts (screenshots/logs), refresh telemetry, update proof README, mark slice done and park.

## Execution Checklist
- [x] Data + utilities added.
- [x] `/dealers` UI implemented with validation + filters.
- [x] Analytics stubs and fallback/outage states added.
- [x] Proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.

## Artefact References
- SCN-004 pack: `specs/references/handoff/SCN-004-dealer-locator/`
- Proof: `specs/proofs/web/WEB-003/README.md`
