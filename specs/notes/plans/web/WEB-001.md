# Plan — WEB-001

- Context:
  Implement the SCN-001 site structure handoff as a Next.js site shell: hero, navigation, CTA linking to dealer flow, bilingual copy (EN/HI), and theme toggle. Use references under `specs/references/handoff/SCN-001-site-structure/` and shared locales.
- Goals:
  - Scaffold `apps/web` (Next.js + Tailwind) to host the marketing shell.
  - Build hero/nav sections referencing locale JSON and brand guardrails.
  - Provide language toggle (EN/HI) and manual light/dark toggle respecting system preference.
  - Include CTA linking to dealer locator placeholder and highlight product families.
  - Capture lint output + screenshots/logs in proof.
- Risks:
  - Theme toggle may flicker without hydration guard; mitigate via CSS + client hook.
  - Importing reference JSON outside project may require duplication; document provenance in code comments.

## Steps
1. Create `apps/web` via `create-next-app`, enable Tailwind + TypeScript + App Router.
2. Add locale data + utility hooks (theme + locale) referencing SCN shared assets.
3. Implement hero/navigation sections with toggles and CTA; add supporting sections (product highlights + dealer CTA) per SCN guidance.
4. Update root scripts/workspaces + `.gitignore`; run `npm run lint -w web` and capture proof artefacts (screenshots/logs, plan references).
5. Refresh telemetry, update proof README, mark slice done, park.

## Execution Checklist
- [x] App scaffolded.
- [x] Hero/nav implemented with toggles + CTA.
- [x] Proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.

## Artefact References
- Source handoff: `specs/references/handoff/SCN-001-site-structure/`
- Locale copy: `_shared/assets/locales/{en,hi}/home.json`
- Proof: `specs/proofs/web/WEB-001/README.md`
