# Plan — WEB-013

- Context:
  Dealer locator map emits basic events; SCN-004 analytics contract requires enriched payloads (coords, postal prefix, channel). Build on WEB-012.
- Goals:
  - Include coordinates + postal info in `dealer_*` events when map pin selected or CTA clicked.
  - Update Playwright tests to inspect dataLayer entries.
- Risks:
  - Need to ensure consent gating remains intact; no event when denied.

## Steps
1. Update analytics dispatcher + dealer map handlers to pass geo data.
2. Extend Playwright tests to check coordinate payload.
3. Capture proof + telemetry, park slice.

## Execution Checklist
- [x] Analytics payload enriched.
- [x] Tests updated.
- [x] Proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.
