# Plan — WEB-004

- Context:
  WEB-003 delivered the `/dealers` shell. SCN-004 still requires automated contract tests for postal validation, outage banner, and analytics events, plus schema checks for dealer data.
- Goals:
  - Extend Playwright suite to cover dealer locator scenarios (valid/invalid postal, filters, outage banner, support fallback, analytics stub assertions).
  - Add schema validation script to ensure dealer data conforms to SCN-004 structure.
  - Wire tests into CI and capture artefacts.
- Risks:
  - Running Playwright twice (SCN-001 + SCN-004) might increase CI time; consider combining suites.
  - Analytics stub assertions rely on console output; ensure deterministic logs.

## Steps
1. Update spec/ledger + supplement (done).
2. Add schema validation script (e.g., `apps/web/tests/dealer-data.spec.ts` or Node script) referencing SCN-004 data.
3. Expand Playwright tests to exercise `/dealers` UI behaviours (invalid postal message, filter chips, outage toggle, support fallback).
4. Update CI to run new suite/logs, capture artefacts + telemetry, park slice.

## Execution Checklist
- [x] Schema validation implemented.
- [x] Playwright tests for `/dealers` added.
- [x] CI updated / proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.
