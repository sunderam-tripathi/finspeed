# Plan — WEB-017

- Context:
  Need accessibility sweep for /dealers per SCN-004 contract.
- Goals:
  - Run axe/Playwright accessibility checks.
  - Fix issues (ARIA labels, keyboard focus, contrast).
- Risks: ensure tests stable.
- Operational note (2025-11-13): any long-running dev server command (e.g., `run-managed web-dev`) must be executed with an explicit timeout wrapper (≤300s) so the console is never blocked indefinitely.

## Steps
1. Run axe audit, note issues.
2. Fix components.
3. Update tests/proof/telemetry.

## Execution Checklist
- [x] Accessibility audit run.
- [x] Fixes applied.
- [x] Proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.
