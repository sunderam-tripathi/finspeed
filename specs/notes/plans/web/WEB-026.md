# Plan — WEB-026

- Context:
  The low-resolution official product cutout remains visibly pixelated and detached even after matte cleanup and environment matching. The hero height also pushes the terrain navigation below the first desktop viewport. The user has now explicitly requested a wholly generated image, removing the earlier exact-source-pixel constraint.
- Goals:
  - Generate complete desktop and mobile hero photographs in which the bicycle and environment are rendered together at high resolution.
  - Retain recognizable Mako Shark color and component cues without reusing the low-resolution cutout in the final image.
  - Keep the full bicycle crisp, physically plausible, naturally lit, and grounded without halos or hard composite edges.
  - Fit header, hero content, technical rail, and terrain navigation within the reference desktop viewport.
  - Preserve current storefront routes, controls, commerce behavior, and consent dismissal.
  - Pass responsive design QA and redeploy through the existing Amplify app.
- Risks:
  - Generative imagery can distort bicycle mechanics or lettering, so outputs require original-resolution inspection and targeted iteration.
  - A shorter hero can compress the headline and commerce row, so the layout must scale fluidly without overlap.
  - Desktop and phone crops require separate art direction to preserve copy-safe space and the entire bicycle.

## Steps
1. Capture and inspect the current production failure, source product cues, live layout, and viewport geometry.
2. Generate and inspect complete desktop and mobile campaign photographs; select only mechanically coherent, naturally integrated outputs.
3. Implement the new responsive assets and revise hero sizing/typography so the complete campaign shell fits the desktop viewport.
4. Verify visual fidelity, responsive behavior, interactions, accessibility, and console health locally.
5. Run repository gates, commit, deploy with Amplify, verify production, complete proof/telemetry, and park the slice.

## Execution Checklist
- [x] Production defect and product-reference cues captured
- [x] Complete desktop hero generated and inspected at original resolution
- [x] Complete mobile hero generated and inspected at original resolution
- [x] Final hero contains no overlaid low-resolution product cutout
- [x] Header, hero content, specifications, and terrain navigation fit the reference desktop viewport
- [x] Desktop and mobile browser screenshots inspected
- [x] Browser interaction, accessibility, and console checks pass
- [x] Lint, build, automated browser tests, and parity checks pass
- [x] `design-qa.md` records `final result: passed`
- [x] Amplify deployment and production verification pass
- [x] Proof and telemetry completed
- [ ] Slice parked
