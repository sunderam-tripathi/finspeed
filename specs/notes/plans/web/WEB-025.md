# Plan — WEB-025

- Context:
  Production review shows that the WEB-024 hero still reads as a pasted cutout: the source bicycle contains bright edge contamination and studio-lit neutral components that conflict with the cold mountain scene. The fixed consent notice also remains visible after a choice, obscuring the bottom of the page.
- Goals:
  - Build a more compatible empty environment and integrate the official Mako Shark with edge cleanup, matched neutral lighting, and wheel-specific contact shadows.
  - Preserve the bicycle geometry, branding, proportions, frame color, and product identity.
  - Keep headline, CTA, price, and specifications visible across responsive breakpoints.
  - Make Accept and Decline dismiss immediately and persist safely when storage is available.
  - Pass side-by-side visual QA and redeploy through the existing Amplify app.
- Risks:
  - Exact source pixels include the visible defect, so changes are limited to photometric matte and neutral-light cleanup; chromatic frame and brand pixels remain protected.
  - A single landscape composition can crop poorly on phones, so desktop and mobile art direction will be validated independently.
  - Storage APIs can throw in restricted browser contexts, so dismissal cannot depend on persistence succeeding.

## Steps
1. Capture and inspect the production defects, official product asset, hero slot, and consent behavior.
2. Generate a compatible empty campaign environment and composite the protected official product with deterministic matte cleanup and matched grounding.
3. Implement the responsive asset and robust consent state handling.
4. Verify desktop/mobile design fidelity, core interactions, accessibility, storage-failure fallback, and console health.
5. Run repository gates, commit, deploy with Amplify, verify production, update proof/telemetry, and park the slice.

## Execution Checklist
- [x] Production defects captured in the proof bundle
- [x] Official bicycle invariants and allowed photometric corrections documented
- [x] Empty environment generated and inspected
- [x] Final desktop/mobile compositions inspected at original resolution
- [x] Consent Accept and Decline both dismiss immediately
- [x] Consent persistence and storage-failure fallback verified
- [x] Browser interaction, accessibility, and console checks pass
- [x] Lint, build, Playwright, and parity checks pass
- [x] `design-qa.md` records `final result: passed`
- [x] Amplify deployment and production verification pass
- [x] Proof and telemetry completed
- [x] Slice parked
