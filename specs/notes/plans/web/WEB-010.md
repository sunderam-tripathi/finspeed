# Plan — WEB-010

- Context:
  SCN-007 blog handoff includes editorial schedule, bilingual posts, CTA expectations. We need a `/blog` landing page summarizing featured content.
- Goals:
  - Build `/blog` route with hero, featured article, recent posts grid, newsletter CTA referencing SCN data.
  - Support EN/HI toggle and include category tags + reading time.
  - Add Playwright smoke test for hero text + featured article.
- Risks:
  - Static data for now; note need for CMS later.

## Steps
1. Extract SCN-007 blog data into `src/data/blog.ts` (locales + posts).
2. Implement `/blog/page.tsx` with hero, featured card, recent posts grid, CTA.
3. Add tests + proof artefacts; run lint/test; refresh telemetry; park slice.

## Execution Checklist
- [x] Data file added.
- [x] `/blog` UI implemented.
- [x] Lint/test + proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.
