# Plan — WEB-015

- Context:
  SCN-004 expects dealer locator data from an API. We currently ship static data; goal is to simulate API fetch.
- Goals:
  - Add mock API route (Next.js API or local JSON) and fetch dealers client-side.
  - Handle errors gracefully, logging analytics/outage events.
- Risks:
  - Need deterministic data for tests.

## Steps
1. Create API stub returning dealer dataset.
2. Update `/dealers` to fetch/refresh data; add loading/error states.
3. Tests + proof + telemetry.

## Execution Checklist
- [x] API stub added.
- [x] `/dealers` fetch logic implemented.
- [x] Tests/lint + proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.
