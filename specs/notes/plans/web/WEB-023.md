# Plan — WEB-023

- Context:
  The live Amplify storefront is functional, but its light homepage has weak contrast and does not express the selected dark mountain-performance direction. The user selected a concrete 1586 x 992 visual target with the Mako Shark bicycle, dark terrain imagery, a compact commerce block, technical specifications, and terrain navigation.
- Goals:
  - Recreate the selected visual target faithfully in the existing `apps/web` storefront.
  - Preserve real Finspeed logo/product assets, catalogue data, navigation, cart, account, and search behavior.
  - Add responsive desktop, tablet, and mobile behavior without degrading accessibility.
  - Pass browser-based design QA against the selected target, then release through the existing Amplify app.
- Risks:
  - The selected mock includes atmospheric terrain photography that is not present in the repository and must be created as a production asset.
  - Homepage-specific dark tokens must not leak into light shop, product, checkout, account, or distributor routes.
  - A faithful desktop composition must collapse cleanly on small screens without hiding the product, price, or primary CTA.

## Steps
1. Capture the selected visual truth, inventory reusable brand/product assets, and align the guarded slice.
2. Create the missing atmospheric hero asset and implement the dark responsive homepage with working navigation and commerce actions.
3. Run lint/build and browser interaction checks, then capture the implementation at the matching viewport.
4. Compare source and implementation, fix all P0/P1/P2 differences, and record a passing `design-qa.md`.
5. Commit, push, deploy the existing Amplify main branch, verify `finspeed.online`, refresh telemetry, and park the slice.

## Execution Checklist
- [x] Selected source visual copied into the proof bundle
- [x] Missing production imagery created and inspected
- [x] Responsive homepage implementation completed
- [x] Existing storefront interactions preserved
- [x] Local lint and production build pass
- [x] Browser interaction and console checks pass
- [x] `design-qa.md` records `final result: passed`
- [ ] Amplify deployment and production browser verification pass
- [ ] Proof artefacts captured and accepted
- [ ] Progress telemetry updated
- [ ] Slice parked after commit and production proof
