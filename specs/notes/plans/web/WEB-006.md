# Plan — WEB-006

- Context:
  SCN-004 contract requires a dealer locator map (IC-12) that visualizes dealer pins, highlights selected card, and supports outage fallback. The current `/dealers` page lacks any map view.
- Goals:
  - Implement map container (Leaflet-like placeholder) that renders dealer pins using screen-reader-friendly SVGs, plus 20km radius overlay.
  - Sync hover/focus between dealer cards and map pins.
  - Respect outage state by hiding map + showing fallback message.
  - Document behaviour and capture screenshots/logs.
- Risks:
  - Without actual Leaflet, use static map but keep API surface to swap later.
  - Focus/ARIA semantics must stay intact.

## Steps
1. Update spec/ledger/supplement (done).
2. Implement map component (`DealerMap`) with SVG or canvas and integrate into `/dealers` page.
3. Add interactions linking cards ↔ pins, including `aria-current` and focus styles; ensure outage state disables map and logs event.
4. Capture artefacts (screenshots/logs), update proof README, refresh telemetry, park.

## Execution Checklist
- [x] Map component implemented.
- [x] Card ↔ pin sync + outage behaviour.
- [x] Proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.
