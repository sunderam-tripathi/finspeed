# Plan — WEB-024

- Context:
  Production review shows that the official Mako Shark cutout and the independently generated mountain background separate visually at real browser dimensions. The bicycle's opaque studio-ground streak, oversized responsive scaling, and a viewport-height hero crop also hide the CTA and price.
- Goals:
  - Build one campaign-ready hero image using an ImageGen environment plate plus the exact official Mako Shark asset.
  - Remove only the old studio-ground residue and preserve every bicycle pixel and proportion.
  - Simplify the browser layout so content remains visible across desktop and mobile.
  - Pass side-by-side visual QA and redeploy the correction through the existing Amplify app.
- Risks:
  - Generative compositing could alter the product, so the bicycle will be composited deterministically after background generation.
  - A single raster hero needs responsive art direction to avoid losing the focal bicycle or text-safe area.
  - Consent UI must remain functional and may overlay first-visit screenshots without being treated as part of the hero.

## Steps
1. Capture the production failure and measure the official product asset and hero slot.
2. Generate an empty dark mountain campaign plate with left-side text-safe negative space.
3. Remove only the cutout's opaque studio floor, composite the unchanged bicycle into the plate, and inspect the final asset.
4. Refactor the hero around the single image, verify desktop/mobile layouts and core interactions, and pass design QA.
5. Run repository gates, commit, deploy with Amplify, verify production, update proof/telemetry, and park the slice.

## Execution Checklist
- [x] Production failure captured in the proof bundle
- [x] Official bicycle invariance documented
- [x] Empty environment plate generated and inspected
- [x] Integrated hero asset composed and inspected
- [x] Responsive layout correction completed
- [x] Browser interaction and console checks pass
- [x] Lint, build, and Playwright tests pass
- [x] `design-qa.md` records `final result: passed`
- [x] Amplify deployment and production verification pass
- [x] Proof and telemetry completed
- [x] Slice parked
