# Plan — WEB-008

- Context:
  SCN-005 brand story pack specifies the hero copy (“Turning Pedals into Power”), mission narrative, team cards, and bilingual requirements. Need `/brand-story` route referencing these assets.
- Goals:
  - Build `/brand-story` page with hero, mission pillars, timeline, quotes, and CTA linking to support/dealer pages.
  - Support EN/HI locales using existing `HOME_COPY` style data from SCN references.
  - Ensure accessibility (ARIA labels, keyboard navigation) and gather proof artefacts.
- Risks:
  - Long content may need responsive layout; ensure tests cover hero text.
  - Locale data duplication—document references in proof.

## Steps
1. Read SCN-005 references and extract hero/testimonial data into `src/data/brand.ts`.
2. Create `/brand-story/page.tsx` with hero + sections (mission, timeline, team, CTA) using bilingual copy.
3. Add nav link or CTA to new route; optionally extend Playwright test for hero text.
4. Capture lint/test logs + screenshots, update proof + telemetry, mark slice done.

## Execution Checklist
- [x] Data file added (`src/data/brand.ts`).
- [x] `/brand-story` UI implemented.
- [x] Tests/lint run; proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.
