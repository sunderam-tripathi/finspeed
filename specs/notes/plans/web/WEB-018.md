# Plan — WEB-018

- Context:
  The landing shell needs a futuristic aesthetic overhaul: gradients, depth, animations, and better contrast for both themes, plus interactive CTAs. We also need to propagate the new brand lockup across all marketing pages.
- Goals:
  - Refresh landing hero/navigation/product cards/support footer with high-end gradients, glassmorphism, and smooth motion.
  - Apply the brand mark component across SCN pages and ensure spacing and contrast meet the design brief.
  - Validate experience via Playwright/lint and capture artefacts for proof.
- Risks:
  - Heavy motion could impact performance/accessibility—need prefers-reduced-motion guards.
  - Global palette changes might regress contrast elsewhere; thorough manual QA required.

## Steps
1. Rebuild landing shell layout from scratch: dark/neo-noir base, bold typography, dual-theme palette tokens.
2. Wire redesigned components (navigation, hero, product families, CTA, footer) with new brand mark, interactions, and motion.
3. Validate (lint + dealer Playwright), capture parity artefacts/screenshots, update proof README & telemetry.

## Execution Checklist
- [x] Proof artefacts captured
- [x] Progress telemetry updated
- [x] Lint + Playwright proof logs stored
- [x] prefers-reduced-motion handling verified
