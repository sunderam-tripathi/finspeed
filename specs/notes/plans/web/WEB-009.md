# Plan — WEB-009

- Context:
  SCN-006 testimonials pack defines rider stories, quotes, and CTA expectations to reinforce trust. Need `/testimonials` page referencing that data.
- Goals:
  - Build `/testimonials` route with bilingual copy, quote cards, video placeholder, CTA.
  - Data should come from `specs/references/handoff/SCN-006-testimonials/...` (encoded into `src/data/testimonials.ts`).
  - Add Playwright smoke test verifying quotes render.
- Risks:
  - Asset references must remain lightweight (no heavy media). Use placeholders.

## Steps
1. Extract testimonial data into `src/data/testimonials.ts` (EN/HI).
2. Implement `/testimonials/page.tsx` with cards, video placeholder, CTA.
3. Update tests + proof, run lint/test, capture artefacts, refresh telemetry, park slice.

## Execution Checklist
- [x] Data file added.
- [x] `/testimonials` UI implemented.
- [x] Lint/test + proof artefacts captured.
- [x] Telemetry refreshed.
- [x] Slice parked.
