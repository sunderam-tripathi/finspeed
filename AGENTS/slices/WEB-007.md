# Slice Supplement — WEB-007

Title: Consent + GA4 integration
Domain: web

Notes:
- Implement consent banner stub (accept/decline) and ensure analytics dispatcher respects preference.
- Push events to `window.dataLayer` + mock GA endpoint per `_shared/contracts/site-interaction-ga4.md`.
- Update tests to simulate both consent paths.
