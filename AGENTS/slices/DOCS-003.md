# Slice Supplement — DOCS-003

Title: Reference integrity automation
Domain: docs

Notes:
- Validates that every `SCN-*` directory appears in `handoff/README.md` and vice versa.
- Blocks CI if references drift; keep regex patterns updated when adding categories.
