# Slice Supplement — WEB-005

Title: Dealer locator analytics
Domain: web

Notes:
- Implement GA4-compliant event dispatcher for `dealer_search_submitted`, `dealer_directions_click`, `dealer_contact_action` referencing `_shared/contracts/site-interaction-ga4.md`.
- Include consent stub (simulate `consentGranted`) and log to `window.dataLayer` + console fallback.
- Expand Playwright tests to assert payload structure via `page.evaluate(() => window.dataLayer)`. Capture proof logs.
